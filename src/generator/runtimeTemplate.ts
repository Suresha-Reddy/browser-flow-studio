export const RUNTIME = String.raw`import { createHash, randomBytes, randomUUID } from "node:crypto";
import { basename, extname } from "node:path";
import { readFile, stat } from "node:fs/promises";
import type { Locator, Page } from "playwright";
import type { DurableRunOptions, FilePayload, FlowCheckpoint, FlowRunResult, HumanInterventionRequest, InterventionResponse, JsonValue, RunOptions, RunResult, RuntimeDocument, SerializableFlowError } from "./types.js";
import definition from "../flow/definition.json" with { type: "json" };
import manifest from "../manifest.json" with { type: "json" };
type Obj=Record<string,any>;
type Ctx={page:Page;input:Obj;documents:Record<string,RuntimeDocument>;callbacks:NonNullable<DurableRunOptions["callbacks"]>;stagehand?:DurableRunOptions["stagehand"];options:DurableRunOptions;checkpoint:FlowCheckpoint};
class FlowFailure extends Error {constructor(public code:string,message:string,public recoverable=true,public stepId?:string){super(message);this.name="FlowFailure"}}
const now=()=>new Date().toISOString();
const safeUrl=(page:Page)=>{try{const u=new URL(page.url());return u.origin+u.pathname}catch{return page.url().split(/[?#]/)[0]}};
const clone=<T>(x:T):T=>JSON.parse(JSON.stringify(x));
const flowChecksum=manifest.flowChecksum;
function firstStep(stateKey:string){return (definition.flow.states as Obj)[stateKey]?.steps?.[0]?.id??null}
function freshCheckpoint(options:DurableRunOptions,stateKey:string):FlowCheckpoint{return{schemaVersion:1,flowKey:definition.application.key,flowVersion:String(definition.application.version),flowChecksum,runId:options.runId??randomUUID(),stateKey,nextStepId:firstStep(stateKey),completedStepIds:[],extracted:{},branchDecisions:{},repeatCounters:{},repeatPositions:{},retryCounters:{},assertionResults:{},manualOverrideStepIds:[],consumedInterventionTokens:[],browserSessionId:options.browserSessionId,pageUrl:safeUrl(options.page),submit:{approved:false,attempted:false},updatedAt:now()}}
function validateCheckpoint(c:FlowCheckpoint){if(c.schemaVersion!==1)throw new FlowFailure("CHECKPOINT_VERSION","Unsupported checkpoint version",false);if(c.flowKey!==definition.application.key||c.flowVersion!==String(definition.application.version)||c.flowChecksum!==flowChecksum)throw new FlowFailure("CHECKPOINT_FLOW_MISMATCH","Checkpoint does not belong to this immutable flow",false);JSON.stringify(c)}
function errorResult(c:Ctx,error:unknown):FlowRunResult{const e=error instanceof FlowFailure?error:new FlowFailure("FLOW_EXECUTION_FAILED",error instanceof Error?error.message:String(error),true,c.checkpoint.nextStepId??undefined);const out:SerializableFlowError={name:e.name,code:e.code,message:e.message,stateKey:c.checkpoint.stateKey,stepId:e.stepId,recoverable:e.recoverable};c.checkpoint.pageUrl=safeUrl(c.page);c.checkpoint.updatedAt=now();return{status:"failed",checkpoint:clone(c.checkpoint),error:out}}
async function emit(c:Ctx,step:string,status:string){c.checkpoint.pageUrl=safeUrl(c.page);c.checkpoint.updatedAt=now();await c.callbacks.onCheckpoint?.({state:c.checkpoint.stateKey,step,status,checkpoint:clone(c.checkpoint)})}
async function uniqueVisible(locator:Locator):Promise<Locator|undefined>{const visible:Locator[]=[];for(let i=0;i<await locator.count();i++){const candidate=locator.nth(i);if((await candidate.isVisible().catch(()=>false))||await candidate.evaluate((n:any)=>n&&n.tagName==="INPUT"&&n.type==="file").catch(()=>false))visible.push(candidate);if(visible.length>1)return}return visible[0]}
async function target(page:Page,t:Obj,stagehand?:DurableRunOptions["stagehand"]):Promise<Locator>{const candidates:Locator[]=[];if(t.testId)candidates.push(page.getByTestId(t.testId));if(t.nameAttribute&&t.placeholder?.length)for(const x of t.placeholder)candidates.push(page.locator("[name="+JSON.stringify(t.nameAttribute)+"][placeholder="+JSON.stringify(x)+"]"));if(t.role)candidates.push(page.getByRole(t.role.type,{name:t.role.name}));for(const x of t.labels??[])candidates.push(page.getByLabel(x,{exact:false}));if(t.nameAttribute)candidates.push(page.locator("[name="+JSON.stringify(t.nameAttribute)+"]"));for(const x of t.placeholder??[])candidates.push(page.getByPlaceholder(x,{exact:false}));for(const x of t.text??[]){candidates.push(page.getByLabel(x,{exact:false}));candidates.push(page.getByPlaceholder(x,{exact:false}));candidates.push(page.locator("label").filter({hasText:x}).locator("input, textarea, select"));candidates.push(page.locator("input[aria-label*="+JSON.stringify(x)+" i], textarea[aria-label*="+JSON.stringify(x)+" i], select[aria-label*="+JSON.stringify(x)+" i]"));candidates.push(page.getByText(x,{exact:false}))}for(const x of t.cssFallbacks??[])candidates.push(page.locator(x));for(const x of candidates){const resolved=await uniqueVisible(x);if(resolved)return resolved}if(stagehand){const seen=await stagehand.page.observe({instruction:"Locate exactly one control matching "+JSON.stringify(t)});const selector=seen[0]?.selector;if(selector){const x=await uniqueVisible(page.locator(selector));if(x)return x}}throw new FlowFailure("TARGET_NOT_UNIQUE","Target not uniquely visible: "+JSON.stringify(t),true)}
const templ=(s:string,input:Obj)=>s.replace(/\{\{fields\.([\w.-]+)\}\}/g,(_,k)=>String(input[k]??""));
async function assertion(c:Ctx,a:Obj):Promise<boolean>{const e=typeof a.expected==="string"?templ(a.expected,c.input):a.expected;if(a.type==="url_contains")return c.page.url().includes(String(e));if(a.type==="visible_text")return await c.page.getByText(String(e),{exact:false}).count()>0;if(a.type==="visible_text_any")return(await Promise.all((e??[]).map((x:string)=>c.page.getByText(x,{exact:false}).count()))).some((n:number)=>n>0);if(a.type==="element_visible")return!!a.target&&await target(c.page,a.target,c.stagehand).then(()=>true).catch(()=>false);if(a.type==="element_hidden")return!a.target||await target(c.page,a.target,c.stagehand).then(()=>false).catch(()=>true);if(a.type==="field_value_equals"||a.type==="selected_value_equals")return!!a.target&&(await(await target(c.page,a.target,c.stagehand)).inputValue())===String(e??"");if(a.type==="download_exists")return Boolean(c.checkpoint.extracted[String(e)]);if(a.type==="extracted_matches")return new RegExp(a.regex).test(String(c.checkpoint.extracted[a.key]??""));return false}
async function assertions(c:Ctx,step:Obj,phase:"pre"|"success"|"failure",list:Obj[]|undefined,any=false){if(!list?.length)return;const values=[];for(let i=0;i<list.length;i++){const value=await assertion(c,list[i]);c.checkpoint.assertionResults[c.checkpoint.stateKey+":"+step.id+":"+phase+":"+i]=value;values.push(value)}if(any?!values.some(Boolean):!values.every(Boolean))throw new FlowFailure("ASSERTION_FAILED",phase+" assertion failed for "+step.id,true,step.id)}
async function detect(page:Page,stateKey:string,stagehand?:DurableRunOptions["stagehand"]){const s=(definition.flow.states as Obj)[stateKey];if(!s)throw new FlowFailure("MISSING_STATE","Missing state "+stateKey,false);const x=s.detect;if(x.urlContains&&!x.urlContains.every((v:string)=>page.url().includes(v)))throw new Error("url");if(x.urlMatches&&!new RegExp(x.urlMatches).test(page.url()))throw new Error("url regex");if(x.titleContains){const title=await page.title();if(!x.titleContains.every((v:string)=>title.includes(v)))throw new Error("title")}for(const text of x.visibleText??[])if(await page.getByText(text,{exact:false}).count()===0)throw new Error("text");for(const t of x.requiredTargets??[])await target(page,t,stagehand)}
async function resolveEntry(page:Page,stagehand?:DurableRunOptions["stagehand"]){const flow=definition.flow as Obj,entryStates=flow.entryStates as string[]|undefined;for(const key of entryStates?.length?entryStates:[flow.initialState as string])try{await detect(page,key,stagehand);return key}catch{}throw new FlowFailure("ENTRY_STATE_NOT_FOUND","No configured entry state matched",true)}
async function recoverPage(c:Ctx){try{await detect(c.page,c.checkpoint.stateKey,c.stagehand);return}catch{}const state=(definition.flow.states as Obj)[c.checkpoint.stateKey],policy=state?.replayPolicy??"manual_only";if(policy==="detect_and_continue"){const detected=await resolveEntry(c.page,c.stagehand);c.checkpoint.stateKey=detected;c.checkpoint.nextStepId=firstStep(detected);return}if(policy==="restart_state"&&state.restartState){await detect(c.page,state.restartState,c.stagehand);c.checkpoint.stateKey=state.restartState;c.checkpoint.nextStepId=firstStep(state.restartState);const ids=new Set(((definition.flow.states as Obj)[state.restartState]?.steps??[]).map((x:Obj)=>x.id));c.checkpoint.completedStepIds=c.checkpoint.completedStepIds.filter(x=>!ids.has(x));return}c.checkpoint.terminalStatus="manual_fallback";throw new FlowFailure("MANUAL_FALLBACK_REQUIRED","Browser page cannot be safely reconstructed for replay policy "+policy,false,c.checkpoint.nextStepId??undefined)}
const mimeFor=(file:string)=>({".pdf":"application/pdf",".jpg":"image/jpeg",".jpeg":"image/jpeg",".png":"image/png"} as Record<string,string>)[extname(file).toLowerCase()]??"application/octet-stream";
async function payloads(c:Ctx,key:string,value:RuntimeDocument):Promise<FilePayload[]>{const values=Array.isArray(value)?value:[value],out:FilePayload[]=[];for(const item of values){let p:FilePayload;if(typeof item==="string")p={name:basename(item),mimeType:mimeFor(item),buffer:await readFile(item)};else p=item;const hash=createHash("sha256").update(p.buffer).digest("hex"),expectedRaw=c.options.resolvedDocuments?.[key],expected=(Array.isArray(expectedRaw)?expectedRaw:[expectedRaw]).filter(Boolean)[out.length];if(expected&&(expected.sha256!==hash||expected.size!==p.buffer.length||expected.mimeType!==p.mimeType||expected.originalFileName!==p.name))throw new FlowFailure("DOCUMENT_METADATA_MISMATCH","Resolved document metadata mismatch for "+key,false);await c.callbacks.onDocumentResolved?.({key,originalFileName:p.name,mimeType:p.mimeType,size:p.buffer.length,sha256:hash,temporaryPath:typeof item==="string"?item:undefined});out.push(p)}return out}
async function validateDocument(key:string,files:FilePayload[]){const d=(definition.documents as Obj[]).find(x=>x.key===key);if(!d)throw new FlowFailure("UNKNOWN_DOCUMENT","Missing document schema "+key,false);if(!d.multiple&&files.length>1)throw new FlowFailure("DOCUMENT_MULTIPLICITY","Document "+key+" does not allow multiple files",false);const mimes=d.acceptedMimeTypes??d.acceptedTypes??[],exts=(d.acceptedExtensions??[]).map((x:string)=>x.toLowerCase());for(const f of files){if(mimes.length&&!mimes.includes(f.mimeType))throw new FlowFailure("DOCUMENT_MIME","Rejected MIME type for "+key,false);if(exts.length&&!exts.includes(extname(f.name).toLowerCase()))throw new FlowFailure("DOCUMENT_EXTENSION","Rejected extension for "+key,false);if(d.maxBytes&&f.buffer.length>d.maxBytes)throw new FlowFailure("DOCUMENT_SIZE","Document exceeds maxBytes for "+key,false)}}
async function existingPayloads(el:Locator):Promise<FilePayload[]>{const rows=await el.evaluate(async(node:any)=>Promise.all(Array.from(node.files??[]).map(async(f:any)=>({name:f.name,mimeType:f.type||"application/octet-stream",bytes:Array.from(new Uint8Array(await f.arrayBuffer()))}))));return(rows as any[]).map(x=>({name:x.name,mimeType:x.mimeType,buffer:Buffer.from(x.bytes)}))}
function requestFor(c:Ctx,step:Obj,intervention:any):HumanInterventionRequest{return{...intervention,resumeToken:intervention.token,runId:c.checkpoint.runId,flowChecksum,prompt:step.prompt??"Review the current browser state and authorize continuation.",pageUrl:safeUrl(c.page)}}
function reasonFor(step:Obj){if(step.type==="payment")return"payment";if(step.type==="human_approval")return"approval";return step.reason==="human_verification"?"visual_verification":step.reason}
async function pause(c:Ctx,step:Obj,parentStepId?:string):Promise<FlowRunResult>{if(!c.checkpoint.intervention){const created=new Date(),ttl=step.expiresInSeconds??(reasonFor(step)==="otp"?600:3600);c.checkpoint.intervention={token:randomBytes(32).toString("base64url"),reason:reasonFor(step),stateKey:c.checkpoint.stateKey,stepId:step.id,parentStepId,createdAt:created.toISOString(),expiresAt:new Date(created.getTime()+ttl*1000).toISOString(),responseAttempts:0}}const req=requestFor(c,step,c.checkpoint.intervention);await c.callbacks.onHumanState?.({status:"paused",request:req});await emit(c,step.id,"paused");return{status:"paused",checkpoint:clone(c.checkpoint),intervention:req}}
async function consumeIntervention(c:Ctx,step:Obj,response:InterventionResponse|undefined,parentStepId?:string):Promise<FlowRunResult|"continue">{const active=c.checkpoint.intervention;if(!active)return pause(c,step,parentStepId) as Promise<FlowRunResult>;if(active.stepId!==step.id||active.parentStepId!==parentStepId)throw new FlowFailure("INTERVENTION_BINDING","Intervention is bound to another step",false,step.id);if(!response)return pause(c,step,parentStepId) as Promise<FlowRunResult>;if(c.checkpoint.consumedInterventionTokens.includes(response.token))return"continue";if(response.token!==active.token)throw new FlowFailure("INTERVENTION_TOKEN","Invalid resume token",false,step.id);if(active.expiresAt&&Date.now()>Date.parse(active.expiresAt))throw new FlowFailure("INTERVENTION_EXPIRED","Intervention expired",false,step.id);if(response.action==="cancel"){c.checkpoint.consumedInterventionTokens.push(active.token);delete c.checkpoint.intervention;c.checkpoint.terminalStatus="cancelled";throw new FlowFailure("INTERVENTION_CANCELLED","Intervention cancelled",false,step.id)}if(step.responsePattern&&response.value?.trim()&&!new RegExp(step.responsePattern).test(response.value.trim())){active.responseAttempts++;if(active.responseAttempts>=(step.maximumAttempts??3))throw new FlowFailure("INTERVENTION_ATTEMPTS","Maximum intervention attempts reached",false,step.id);return pause(c,step,parentStepId) as Promise<FlowRunResult>}if(step.type==="human_input"&&response.value?.trim()){if(!step.target)throw new FlowFailure("INTERVENTION_VALUE","A target is required when the value is entered through Flow Studio",false,step.id);await(await target(c.page,step.target,c.stagehand)).fill(response.value.trim())}if(step.type==="human_approval"){if(response.approved!==true)throw new FlowFailure("APPROVAL_REJECTED","Approval was not granted",false,step.id);c.checkpoint.submit.approved=true}if(!["human_input","human_approval","payment"].includes(step.type))c.checkpoint.manualOverrideStepIds.push(step.id);c.checkpoint.consumedInterventionTokens.push(active.token);const req=requestFor(c,step,active);delete c.checkpoint.intervention;await c.callbacks.onHumanState?.({status:"resumed",request:req});return"continue"}
async function mismatchPause(c:Ctx,step:Obj,response?:InterventionResponse):Promise<FlowRunResult|undefined>{if(!["fill","date","select"].includes(step.type)||step.mode!=="fill_if_empty"||step.existingValuePolicy!=="manual_review_on_mismatch"||c.checkpoint.manualOverrideStepIds.includes(step.id))return;const el=await target(c.page,step.target,c.stagehand),current=(await el.inputValue()).trim(),incoming=String(c.input[step.field]??"").trim();if(current&&current!==incoming){const synthetic={...step,reason:"visual_verification",prompt:"Existing value differs from expected input. Review the current applicant before continuing."};const result=await consumeIntervention(c,synthetic,response);if(result!=="continue")return result}}
async function action(c:Ctx,step:Obj,stateSteps:Obj[]):Promise<string|undefined>{if(step.type==="navigate"){await c.page.goto(step.url,{waitUntil:"domcontentloaded"});return}if(step.type==="assert_page")return;if(step.type==="wait_for"){const end=Date.now()+step.timeoutMs;while(Date.now()<end){if(await assertion(c,step.assertion))return;await c.page.waitForTimeout(250)}throw new FlowFailure("WAIT_TIMEOUT","wait_for timeout",true,step.id)}if(step.type==="branch"){const next=step.cases[String(c.input[step.basedOn]??c.checkpoint.extracted[step.basedOn]??"")]??step.cases.default;c.checkpoint.branchDecisions[step.id]=next;return next}if(step.type==="capture_evidence"){const p=(c.options.artifactDirectory??".")+"/"+step.evidence+".png";await c.page.screenshot({path:p,fullPage:step.fullPage??true});await c.callbacks.onEvidence?.({name:step.evidence,path:p,state:c.checkpoint.stateKey,step:step.id});return}const el=await target(c.page,step.target,c.stagehand);if(step.type==="fill"||step.type==="date"){const current=await el.inputValue(),incoming=String(c.input[step.field]??"");if(step.mode==="preserve")return;if(step.mode==="fill_if_empty"&&current.trim()){if(step.existingValuePolicy==="require_match"&&current.trim()!==incoming.trim())throw new FlowFailure("EXISTING_VALUE_MISMATCH","Existing value does not match expected input",false,step.id);return}await el.fill(step.type==="fill"&&step.mode==="append"?current+incoming:incoming);return}if(step.type==="select"){const current=await el.inputValue(),incoming=String(c.input[step.field]??"");if(step.mode==="preserve")return;if(step.mode==="fill_if_empty"&&current){if(step.existingValuePolicy==="require_match"&&current!==incoming)throw new FlowFailure("EXISTING_VALUE_MISMATCH","Existing selection does not match expected input",false,step.id);return}await el.selectOption(incoming);return}if(step.type==="checkbox"){step.checked?await el.check():await el.uncheck();return}if(step.type==="radio"){await el.check();return}if(step.type==="click"){await el.click();return}if(step.type==="upload"){const value=c.documents[step.document];if(!value)throw new FlowFailure("DOCUMENT_MISSING","Missing document "+step.document,false,step.id);const incoming=await payloads(c,step.document,value);await validateDocument(step.document,incoming);const mode=step.uploadMode??"replace";const isFileInput=await el.evaluate((n:any)=>n&&n.tagName==="INPUT"&&n.type==="file").catch(()=>false);if(!isFileInput){const[chooser]=await Promise.all([c.page.waitForEvent("filechooser"),el.click()]);await chooser.setFiles(incoming);return}const count=await el.evaluate((n:any)=>n.files?.length??0);if(mode==="preserve_if_present"&&count>0)return;if(mode==="replace"){await el.setInputFiles([]);await el.setInputFiles(incoming)}else{const existing=await existingPayloads(el);await el.setInputFiles([...existing,...incoming])}return}if(step.type==="extract"){const x=(await el.innerText()).trim();if(step.required&&!x)throw new FlowFailure("EXTRACTION_MISSING","Missing extraction "+step.key,true,step.id);if(step.regex&&!new RegExp(step.regex).test(x))throw new FlowFailure("EXTRACTION_INVALID","Invalid extraction "+step.key,true,step.id);if(step.checkpointPolicy==="redact")c.checkpoint.extracted[step.key]="[REDACTED]";else if(step.checkpointPolicy!=="omit")c.checkpoint.extracted[step.key]=x;return}if(step.type==="download"){const dir=c.options.artifactDirectory??".";const ctx=c.page.context();const wait=Promise.race([c.page.waitForEvent("download",{timeout:60000}).catch(()=>undefined),ctx.waitForEvent("page",{timeout:60000}).then(pg=>pg.waitForEvent("download",{timeout:60000})).catch(()=>undefined)]);await el.click();const d=await wait;if(!d)throw new FlowFailure("DOWNLOAD_NOT_STARTED","No download started for "+step.artifact,true,step.id);const failure=await d.failure();if(failure)throw new FlowFailure("DOWNLOAD_FAILED","Download failed for "+step.artifact+": "+failure,true,step.id);const p=dir+"/"+(d.suggestedFilename()||step.artifact);await d.saveAs(p);c.checkpoint.extracted[step.artifact]=p;return}if(step.type==="submit"){if(!c.options.allowSubmit||!c.checkpoint.submit.approved)throw new FlowFailure("SUBMIT_NOT_AUTHORIZED","Final submission not authorized",false,step.id);if(c.checkpoint.submit.attempted&&!c.checkpoint.submit.resultReference)throw new FlowFailure("SUBMIT_OUTCOME_UNKNOWN","Submit was already attempted and cannot be replayed",false,step.id);c.checkpoint.submit.attempted=true;c.checkpoint.submit.idempotencyKey??=randomUUID();await emit(c,step.id,"side_effect_intent");await el.click();return}throw new FlowFailure("STEP_UNSUPPORTED","Unsupported step "+step.type,false,step.id)}
async function runAtomic(c:Ctx,step:Obj,stateSteps:Obj[],instanceId=step.id,response?:InterventionResponse):Promise<{next?:string;paused?:FlowRunResult}>{if(c.checkpoint.completedStepIds.includes(instanceId))return{};if(["human_input","human_approval","payment"].includes(step.type)){const r=await consumeIntervention(c,step,response);if(r!=="continue")return{paused:r};await assertions(c,step,"failure",step.failureAny,true).then(()=>{if(step.failureAny?.length)throw new FlowFailure("FAILURE_ASSERTION","Failure condition matched",true,step.id)}).catch(e=>{if(e instanceof FlowFailure&&e.code==="ASSERTION_FAILED")return;throw e});await assertions(c,step,"success",step.success);await assertions(c,step,"success",step.successAny,true);c.checkpoint.completedStepIds.push(instanceId);await emit(c,step.id,"completed");return{}}const manual=await mismatchPause(c,step,response);if(manual)return{paused:manual};const max=step.retry?.maximumAttempts??1;for(let attempt=(c.checkpoint.retryCounters[instanceId]??0)+1;attempt<=max;attempt++){c.checkpoint.retryCounters[instanceId]=attempt;try{await emit(c,step.id,"started");await assertions(c,step,"pre",step.preconditions);await assertions(c,step,"failure",step.failureAny,true).then(()=>{if(step.failureAny?.length)throw new FlowFailure("FAILURE_ASSERTION","Failure condition matched",true,step.id)}).catch(e=>{if(e instanceof FlowFailure&&e.code==="ASSERTION_FAILED")return;throw e});const next=await action(c,step,stateSteps);await assertions(c,step,"success",step.success);await assertions(c,step,"success",step.successAny,true);if(step.type==="submit")c.checkpoint.submit.resultReference=safeUrl(c.page);c.checkpoint.completedStepIds.push(instanceId);await emit(c,step.id,"completed");return{next}}catch(error){await emit(c,step.id,attempt===max?"failed":"retrying");if(attempt===max)throw error}}return{}}
async function runRepeat(c:Ctx,step:Obj,stateSteps:Obj[],response?:InterventionResponse):Promise<{paused?:FlowRunResult}>{if(c.checkpoint.completedStepIds.includes(step.id))return{};let iteration=c.checkpoint.repeatCounters[step.id]??0;while(iteration<step.maximumIterations){if(await assertion(c,step.until)){c.checkpoint.completedStepIds.push(step.id);await emit(c,step.id,"completed");return{}}let pos=c.checkpoint.repeatPositions[step.id]??0;for(;pos<step.steps.length;pos++){const child=stateSteps.find(x=>x.id===step.steps[pos]);if(!child||["repeat","branch","submit"].includes(child.type))throw new FlowFailure("REPEAT_CHILD_INVALID","Invalid repeat child "+step.steps[pos],false,step.id);const instance=step.id+":"+iteration+":"+child.id;const r=await runAtomic(c,child,stateSteps,instance,response);response=undefined;if(r.paused){c.checkpoint.repeatPositions[step.id]=pos;return{paused:r.paused}}c.checkpoint.repeatPositions[step.id]=pos+1}iteration++;c.checkpoint.repeatCounters[step.id]=iteration;c.checkpoint.repeatPositions[step.id]=0;await emit(c,step.id,"iteration_completed")}throw new FlowFailure("REPEAT_MAXIMUM","Repeat maximum reached",true,step.id)}
export async function runApplicationFlowUntilPause(options:DurableRunOptions):Promise<FlowRunResult>{let checkpoint:FlowCheckpoint=freshCheckpoint(options,definition.flow.initialState);try{if(options.checkpoint){checkpoint=clone(options.checkpoint);validateCheckpoint(checkpoint)}else{await options.page.goto(definition.application.entryUrl,{waitUntil:"domcontentloaded"});const entry=await resolveEntry(options.page,options.stagehand);checkpoint=freshCheckpoint(options,entry)}const c:Ctx={page:options.page,input:options.input,documents:options.documents??{},callbacks:options.callbacks??{},stagehand:options.stagehand,options,checkpoint};if(options.checkpoint)await recoverPage(c);if(options.interventionResponse&&checkpoint.consumedInterventionTokens.includes(options.interventionResponse.token))options={...options,interventionResponse:undefined};while(true){if((definition.flow.terminalStates as string[]).includes(checkpoint.stateKey)){checkpoint.terminalStatus="completed";checkpoint.nextStepId=null;checkpoint.updatedAt=now();return{status:"completed",checkpoint:clone(checkpoint),finalState:checkpoint.stateKey,extracted:clone(checkpoint.extracted)}}const state=(definition.flow.states as Obj)[checkpoint.stateKey];if(!state)throw new FlowFailure("MISSING_STATE","Missing state "+checkpoint.stateKey,false);if(checkpoint.nextStepId==null){const next=state.transitions.success??"completed";checkpoint.stateKey=next;checkpoint.nextStepId=firstStep(next);continue}const index=state.steps.findIndex((x:Obj)=>x.id===checkpoint.nextStepId);if(index<0)throw new FlowFailure("CHECKPOINT_STEP_MISSING","Checkpoint step is not in current state",false,checkpoint.nextStepId);const step=state.steps[index];let result:{next?:string;paused?:FlowRunResult};if(step.type==="repeat")result=await runRepeat(c,step,state.steps,options.interventionResponse);else result=await runAtomic(c,step,state.steps,step.id,options.interventionResponse);options={...options,interventionResponse:undefined};if(result.paused)return result.paused;if(result.next){checkpoint.stateKey=result.next;checkpoint.nextStepId=firstStep(result.next)}else checkpoint.nextStepId=state.steps[index+1]?.id??null}}catch(error){const c={page:options.page,input:options.input,documents:options.documents??{},callbacks:options.callbacks??{},stagehand:options.stagehand,options,checkpoint:checkpoint!} as Ctx;return errorResult({...c,checkpoint},error)}}
export async function runApplicationFlow(options:RunOptions):Promise<RunResult>{let checkpoint=options.checkpoint,response=options.interventionResponse;while(true){const result=await runApplicationFlowUntilPause({...options,checkpoint,interventionResponse:response});checkpoint=result.checkpoint;response=undefined;if(result.status==="completed")return{status:result.status,state:result.finalState,extracted:result.extracted,checkpoint};if(result.status==="failed")throw Object.assign(new Error(result.error.message),result.error);if(!options.callbacks?.onHumanIntervention)throw new Error("Human intervention callback required");const legacy=await options.callbacks.onHumanIntervention(result.intervention);response={token:result.intervention.token,action:legacy.status==="cancelled"?"cancel":"resume",value:legacy.value,approved:legacy.approved}}}
export { definition };
export { HumanGate } from "./human-gate.js";
`;
